{
  "name": "cinetalks.com.br",
  "version": "1.0.0",
  "description": "Implementando um projeto de grande escala do zero",
  "main": "index.js",
  "scripts": {
    "dev": "next dev & docker compose -f infra/compose.yaml up -d & psql --host=localhost --username=postgres --port=5432",
    "lint-check": "prettier --check .",
    "lint-fix": "prettier --write .",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "next": "^13.1.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "jest": "^29.6.2",
    "prettier": "^3.0.0"
  }
}
